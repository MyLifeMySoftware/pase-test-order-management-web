spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pase_db
    username: owner
    password: Owner123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  data:
    jpa:
      repositories:
        enabled: true